<div class="homework-show-body">

  <% done = @tasks.count { |task| current_user.completed?(task) } %>
  <% total = @tasks.size %>

  <div class="show-summary-row">

    <div class="show-summary-incomplete-card">
      <span class="show-summary-num" id="showDoneCount">
        <%= done %>
      </span>
      <span class="show-summary-label">未完了</span>
    </div>

    <div class="show-summary-complete-card">
      <span class="show-summary-num" id="showTotalCount">
        <%= total %>
      </span>
      <span class="show-summary-label">完了</span>
    </div>

  </div>

  <!-- =========================
      タスク一覧
  ========================= -->
  <div class="task-index-list">
    <div class="task-index-list-header">
      <h3><%= current_user.classroom.name %> 宿題リスト</h3>
      <span class="task-index-list-count"><%= total %> タスク</span>
    </div>

    <% if @homeworks.any? %>
      <div class="test-period-banner">
        📅 テスト期間：
        <%= @homeworks.minimum(:test_start_date).strftime("%m月%d日") %> 〜
        <%= @homeworks.maximum(:test_end_date).strftime("%m月%d日") %>
      </div>
    <% else %>
      <div class="test-period-banner">
        📅 現在テスト期間はありません
      </div>
    <% end %>
    <div class="test-index-info">
      <ul>
        <li>📌 授業前に宿題を必ずチェックしよう！</li>
        <li>✅ 宿題が終わったらチェックを入れよう！</li>
      </ul>
    </div>

    <!-- タブ -->
    <div class="task-index-tabs">
      <button class="task-index-tab active" data-tab="all">すべて</button>
      <button class="task-index-tab" data-tab="incomplete">未完了</button>
      <button class="task-index-tab" data-tab="completed">完了</button>
    </div>

    <% @tasks.each do |task| %>
    <div
      id="task-<%= task.id %>"
      class="task-index-item <%= 'completed' if current_user.completed?(task) %>"
      data-status="<%= current_user.completed?(task) ? 'completed' : 'incomplete' %>">

      <label class="task-index-check-wrap">
        <div id="completed-button-for-task-<%= task.id %>">
          <%= render 'tasks/completed_button', task: task %>
        </div>
        <span class="task-index-name">
          <%= task.name %>
        </span>
        <% if task.pdf.attached? %>
          <%= link_to url_for(task.pdf), target: "_blank", class: "homework-show-pdf-link" do %>
            <i class="fas fa-file-pdf"></i>
            file
          <% end %>
        <% end %>
      </label>
      
    </div>
  <% end %>

  <% if @tasks.empty? %>
    <div>タスクはありません。</div>
  <% end %>
  </div>
</div>
